node_modules
dist
dev-dist
coverage
package-lock.json
bridge/package-lock.json

# Prettier can infer no parser for .npmrc. `prettier --check .` skips it, but the glob
# forms (`--check "**"`) hard-error on it, so pin the behaviour rather than depend on
# how the format script happens to be spelled.
.npmrc

# Vendored upstream artifacts — never hand-edited, never reformatted.
vendor
.impeccable

# Synced canon from jrmoulckers/.github — reformatting creates false sync drift.
.github/agents
.github/skills
.github/prompts
.github/instructions

# Partly upstream-owned: the studio:base region is merged in by the sync engine.
AGENTS.md
